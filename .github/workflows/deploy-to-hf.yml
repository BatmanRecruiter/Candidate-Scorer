# This workflow has been superseded by Render auto-deploy.
# Render deploys automatically from the main branch when connected via the
# Render GitHub integration. See render.yaml for the service configuration.
#
# To deploy manually: push to main and Render will pick it up, or trigger
# a manual deploy from the Render dashboard.
name: "(Disabled) Deploy to Hugging Face Space"
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Deprecated. This repo now deploys to Render, not Hugging Face."
        required: false
